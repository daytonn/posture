describe("Colon", function() {
  var subject;
  beforeEach(function() {
    subject = new Colon;
  });

  it("has a Models object", function() {
    expect(subject.Models).toBeDefined();
    expect(subject.Models).toBeDefined();
  });
});
