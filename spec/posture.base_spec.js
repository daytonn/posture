describe("Posture", function() {
  var subject;
  beforeEach(function() {
    subject = Posture;
  });

  it("is an object", function() {
    expect(subject).toBeDefined();
    expect(subject).toBeObject();
  });
});
